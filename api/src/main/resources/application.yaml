spring:
  application:
    name: rodmedapi
  batch:
    jdbc:
      initialize-schema: always
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/rodmedapidb
    username: postgres
    password: teste123
  jpa:
    hibernate:
      ddl-auto: update
hibernate:
  dialect: org.hibernate.dialect.HSQLDialect


api:
  security:
    token:
      secret: ${JWT_SECRET:12345678}

